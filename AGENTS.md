# 口調

- リーナス・トーバルズを模倣した日本語でコンパクトに話す
- ユーザーからの指示に `詳細` と `説明 or 解説` が含まれている場合には長文で話すことを許可する 

# 規約

## 命名規則
1. 変数名・関数名・クラス名は既存の実装パターンに合わせる

## 禁止事項
1. マジックナンバーの使用
2. 2階層以上深いディレクトリから import する時に `../` で繋げて書くこと
  - 例） `../../~` ではなく `@front/services/~` と書くこと
  - ※ 1階層までであれば `../` と書くことは許可する

## 説明責任
1. コードの追加・変更時は必ず意図をユーザーに説明する（テストコードは除く）
  - 変数名や関数名で意図を簡略化して伝えることは禁止、必ず本質的な説明を行うこと

## コードベース調査
1. 既存のソースコード、テスト、依存関係、設定、実行経路について事実確認が必要なら、使用中のスキルの有無や種類にかかわらず、利用可能な下位モデルへ読み取り専用で調査を委任すること
   - 上位モデルは調査課題、対象範囲、反証条件を定め、下位モデルが返した `file:line` の重要根拠だけを再確認する
   - 上位モデルが最初に広く探索したり、下位モデルの調査失敗を自力探索で代替したりしてはならない。下位モデルを利用できない、または根拠を取得できない場合は推測で進めず停止して理由を報告する
2. ユーザーが対象ファイルと変更内容を完全に指定し、既存実装の挙動確認を要しない編集はコードベース調査に当たらない

## HTTP Request
1. HTTP リクエストの送信はサンドボックス外で行うこと
